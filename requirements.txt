# Apples-vs-Oranges replication — pinned deps.
# Versions chosen to be compatible with each other and with bert-score's
# transformers contract as of late 2024 / early 2025.

# Data + utils
pandas>=2.1.0
tqdm>=4.66.0
requests>=2.31.0

# Metrics (Task 4)
sacrebleu>=2.4.0
rouge-score>=0.1.2
nltk>=3.9.0
bert-score>=0.3.13

# LLM judge — local backend (Task 3)
torch>=2.2.0
transformers>=4.45.0
accelerate>=0.34.0
bitsandbytes>=0.43.0  # NOTE: not installable on macOS; required only for --backend local on the HPC node

# Tests
pytest>=8.0.0
